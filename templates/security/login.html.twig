{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
			<!--Inner Home Banner Start-->
			<div class="wt-haslayout wt-innerbannerholder">
				<div class="container">
					<div class="row justify-content-md-center">
						<div class="col-xs-12 col-sm-12 col-md-8 push-md-2 col-lg-6 push-lg-3">
							<div class="wt-innerbannercontent">
							<div class="wt-title"><h2>Se connecter</h2></div>
							<ol class="wt-breadcrumb">
								<li><a href="{{ path("home") }}">Accueil</a></li>
								<li class="wt-active">Se connecter</li>
							</ol>
							</div>
						</div>
					</div>
				</div>
			</div>
			<!--Inner Home End-->
			<!--Main Start-->                
			<main id="wt-main" class="wt-main wt-haslayout wt-innerbgcolor">
				<!--Register Form Start-->
				<div class="wt-haslayout wt-main-section">
					<div class="container">
						<div class="row justify-content-md-center">
							<div class="col-xs-12 col-sm-12 col-md-10 push-md-1 col-lg-8 push-lg-2">
								<div class="wt-registerformhold">
									<div class="wt-registerformmain">
										<div class="wt-registerhead">
											<div class="wt-title">
												<h3>Se connecter</h3>
											</div>
											<div class="wt-description">
												<p>« Jamais de telles épreuves ne se surmontent en solitaire. C’est au contraire en solidaires, en disant « nous » plutôt qu’en pensant « je » que nous relèverons cet immense défi. »</p>
											</div>
										</div>
                                        {% if app.user %}
                                            <div class="mb-3 text-center">
                                                vous êtes déjà connecter avec l'adresse mail : <b>{{ app.user.username }}</b>
                                                </br>
                                                <a href="{{ path('app_logout') }}" class="wt-btn">Se déconnecter</a>
                                            </div>
                                        {% else %}
										<div class="wt-joinforms">
											<form class="wt-formtheme wt-formregister" method="post">
												<fieldset class="wt-registerformgroup">
													<div class="form-group">
														<label>Votre adresse email :</label>
														<input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus>
													</div>
													<div class="form-group">
														<label>Votre mot de passe :</label>
														<input type="password" name="password" id="inputPassword" class="form-control" required>
													</div>
                                                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
													<div class="form-group text-center">
														<button class="wt-btn">Se connecter</button>
													</div>
												</fieldset>
											</form>
										</div>
                                        {% endif %}
									</div>
									<div class="wt-registerformfooter">
										<span>Pas encore de compte? <a href="javascript:void(0);">S'enregistrer</a></span>
									</div>
								</div>
							</div>
						</div>
					</div>
				</div>
				<!--Register Form End-->
			</main>
			<!--Main End-->
{% endblock %}

