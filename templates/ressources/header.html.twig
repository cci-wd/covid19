<header id="wt-header" class="wt-header wt-haslayout">
				<div class="wt-navigationarea">
					<div class="container-fluid">
						<div class="row">
							<div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
								<strong class="wt-logo"><a href="{{ path("home") }}"><img src="images/logo.png" alt="company logo here"></a></strong>
								<div class="wt-rightarea">
									<nav id="wt-nav" class="wt-nav navbar-expand-lg">
										<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
											<i class="lnr lnr-menu"></i>
										</button>
										<div class="collapse navbar-collapse wt-navigation" id="navbarNav">
											<ul class="navbar-nav">
												<li class="nav-item">
													<a href="{{ path("asklisting") }}">Trouver une mission</a>
												</li>
                                                <li class="nav-item">
													<a href="howitworks.html">Mes missions</a>
												</li>
                                                <li class="nav-item">
													<a href="{{ path("securityrules") }}">Règles de sécurité</a>
												</li>
											</ul>
										</div>
									</nav>
									{% if app.user %}
									<div class="wt-loginarea">
										<a href="{{ path("app_logout") }}" class="wt-btn" style="background-color: white; color: black;">Se déconnecter</a>
										<a href="{{ path("ask_new") }}" class="wt-btn">Créer une mission</a>
									</div>
									{% else %}
									<div class="wt-loginarea">
										<a href="{{ path("app_login") }}" class="wt-btn" style="background-color: white; color: black;">Se connecter</a>
										<a href="{{ path("app_register") }}" class="wt-btn">Créer un compte</a>
									</div>
									{% endif %}
								</div>
							</div>
						</div>
					</div>
				</div>
			</header>
